<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="1" author="alexander">
        <createTable tableName="media">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="url" type="varchar(4096)">
                <constraints nullable="false"/>
            </column>
            <column name="type" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="resolution" type="varchar(10)">
                <constraints nullable="false"/>
            </column>
            <column name="format" type="varchar(7)">
                <constraints nullable="false"/>
            </column>
            <column name="duration_seconds" type="integer"/>
        </createTable>

        <!-- addCheckConstraint is not available in open-source Liquibase -->
        <sql>
            ALTER TABLE media
                ADD CONSTRAINT check_media_type_duration
                    CHECK ((type = 'video' AND duration_seconds IS NOT NULL)
                        OR (type = 'image' AND duration_seconds IS NULL));
        </sql>
    </changeSet>
</databaseChangeLog>